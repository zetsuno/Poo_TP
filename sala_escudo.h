#include "sala.h"

class sala_escudo
{
	int escudo;
public:
	sala_escudo();
	~sala_escudo();
	int getEscudo() const;
	void setEscudo(const int &escudo);
};

sala_escudo::sala_escudo()
{
}

sala_escudo::~sala_escudo()
{
}